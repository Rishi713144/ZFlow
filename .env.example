# Database
DB_USER=user
DB_PASSWORD=password
DB_NAME=zflow

# Security
JWT_SECRET=your-super-secret-key

# Public URLs (Used for local dev context)
PUBLIC_BACKEND_URL=http://localhost:3001
PUBLIC_HOOKS_URL=http://localhost:3002

# Domain Mapping (For Docker)
FRONTEND_DOMAIN=localhost
BACKEND_DOMAIN=api.localhost
HOOKS_DOMAIN=hooks.localhost

# External Services (Optional)
SOL_PRIVATE_KEY=
SMTP_PASSWORD=
